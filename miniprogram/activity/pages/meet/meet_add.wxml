<view class="main main-info-add main-skin1" wx:if="{{isLogin}}">
 	<import src="meet_form_tpl.wxml" />

 	<template is="infoForm" data="{{skin,typeOptions,expireStart,imgMax,imgList,formTypeIndex,formTitle,formTitleFocus,formContent,formContentFocus,formExpireTime,formRegion,formFee,formFeeFocus,formDay,formPersonMax,formPersonMaxFocus,formContact,formContactFocus}}" />
 </view>